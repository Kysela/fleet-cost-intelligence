# Railway Service Configuration  

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run preview -- --host 0.0.0.0 --port $PORT --strictPort false"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
